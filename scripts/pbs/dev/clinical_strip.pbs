#!/bin/bash
#PBS -N clinstrip
#PBS -P 11704243
#PBS -l select=1:ncpus=16:mem=64gb
#PBS -l walltime=02:00:00
#PBS -j oe
#PBS -o logs/clinical_strip.log
#
# 论文 R6 的临床侧:剥离水平之后那 20 个指标还剩多少。
# 纯 CPU,零 GPU -> qdev(PITFALLS 第 7 条:login node 上不碰 python;第 8 条:写 mem=)。
#
#   qsub scripts/pbs/dev/clinical_strip.pbs
source "${REPO:-$HOME/workspace/Project_CGM/CGM-OutlierMIA-master}/scripts/pbs/env.sh"
conda activate "$ENV_PREFIX"; cd "$REPO"
set -euo pipefail
mkdir -p logs
export PYTHONPATH="$REPO/scripts:${PYTHONPATH:-}"
python -u scripts/clinical_after_strip.py
