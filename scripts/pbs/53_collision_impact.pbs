#!/bin/bash
#PBS -N cgm_collide
#PBS -P 11704243
#PBS -j oe
#
# `id` is unique only within a source study. How much of the published cohort, the
# outlier consensus and the identifiability result rests on composite subjects?
#
#   qsub -l select=1:ncpus=8:mem=32gb -l walltime=00:30:00 \
#        -o logs/53_collision_impact.log scripts/pbs/53_collision_impact.pbs   # -> qdev
source "${REPO:-$HOME/workspace/Project_CGM/CGM-OutlierMIA-master}/scripts/pbs/env.sh"
conda activate "$ENV_PREFIX"
python -u scripts/check_collision_impact.py 2>&1 | tee logs/53_collision_impact.live.log
